# Neon Retro

A local browser project by TEKDEV.

## Product

Neon Retro is an Android browser concept with tabs, bookmarks, history, downloads, notifications, private browsing controls, fingerprint masking controls, location simulation controls, cookie controls, cache controls, and site permission controls.

## First build

The first build starts with the local React control layer.
